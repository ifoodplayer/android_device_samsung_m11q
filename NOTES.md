Notes for Build this branch
=================================================

OBS: I used the LineageOS 17.1 Revived fork (Maybe this won't happen if you use the official version), if you are going to build it check if ro.zygote is 64_32 in out/target/product/m11q/vendor/default.prop if it is 32 put 64_32 do this before it builds the vendor image
